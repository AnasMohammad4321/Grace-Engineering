<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inventory</title>
  <style>
    /* Reset default margin and padding */
    body, h1, table {
      margin: 0;
      padding: 0;
    }

    /* Set background color and text styles */
    body {
      background-color: #f0f0f0;
      font-family: Arial, sans-serif;
      color: #333;
    }

    /* Header styles */
    h1 {
      background-color: #333;
      color: #fff;
      padding: 20px;
      text-align: center;
    }

    /* Table styles */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      background-color: #fff;
    }

    th, td {
      padding: 10px;
      text-align: left;
      border-bottom: 1px solid #ccc;
    }

    th {
      background-color: #333;
      color: #fff;
    }

    /* Table row hover effect */
    tbody tr:hover {
      background-color: #f5f5f5;
    }

    /* Table cell alignment */
    .center-align {
      text-align: center;
    }

    /* Table cell width */
    .wide-cell {
      width: 150px;
    }
  </style>
</head>
<body>
  <h1>Inventory</h1>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Description</th>
        <th>Quantity</th>
        <th>Price</th>
        <th class="center-align wide-cell">Created At</th>
        <th class="center-align wide-cell">Updated At</th>
      </tr>
    </thead>
    <tbody>
      <% @inventories.each do |inventory| %>
        <tr>
          <td><%= inventory.id %></td>
          <td><%= inventory.name %></td>
          <td><%= inventory.description %></td>
          <td><%= inventory.quantity %></td>
          <td class="center-align"><%= inventory.created_at %></td>
          <td class="center-align"><%= inventory.updated_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
